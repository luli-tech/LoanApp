import React from "react";
import "./spinner.css"; // This is where we define the spinner styles

const Spinner = () => {
    return (
        <div className="spinner-container">
            <div className="spinner"></div>
        </div>
    );
};

export default Spinner;
